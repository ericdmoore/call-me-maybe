{
  "name": "callmemaybe-site",
  "private": true,
  "type": "module",
  "description": "callmemaybe.cc \u2014 the project site",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "cf:preview": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "deploy:redirect": "wrangler deploy -c wrangler.redirect.jsonc",
    "cf:whoami": "wrangler whoami"
  },
  "devDependencies": {
    "astro": "^7.1.1",
    "wrangler": "^4.115.0"
  },
  "overrides": {
    "sharp": "^0.35.3"
  }
}
