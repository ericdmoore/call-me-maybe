; Hold music. Debian: `sudo apt install asterisk-moh-opsound-wav` drops
; sample files into /usr/share/asterisk/moh (symlinked under this dir).
; Or point `directory` at your own folder of 8 kHz mono WAVs — same
; no-transcode rule as the lobby prompts.

[default]
mode = files
directory = moh
