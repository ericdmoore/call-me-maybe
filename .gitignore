# Secrets and local state — never commit these.
.env
policy.toml
handsets.toml
asterisk/pjsip.conf
asterisk/ari.conf
# Rendered output: pjsip_handsets.conf contains real SIP passwords.
asterisk/generated/

# Built prompt audio (regenerate with `bun run prompts:build`)
prompts/build/

bin/
*.log
.DS_Store
